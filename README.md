# 小鲁班桌面宠物

一款面向 Windows 11 的离线 WPF 桌面宠物。小鲁班会趴在枕头上打呼噜、响应点击、在屏幕边缘探头，支持待办事项、循环定时提醒、免打扰、开机自启、多屏和高 DPI。

当前工作树源码版本：`v1.0.65`，基于用户指定的 `v1.0.48` 发布程序恢复并开发。程序不需要联网、账号或后台服务，当前版本也不会创建文件日志。GitHub Release 可能晚于源码；下载用户应以具体 Release 页的版本号和说明为准。

## 快速开始

1. 从 [GitHub Releases](https://github.com/guoqihan342-svg/xlb-pet/releases) 下载目标版本的 `LubanDesktopPet.exe`，并先阅读该版本说明。仓库中的 `dist/` 是本地发布目录，不随 Git 克隆分发。
2. 安装 Windows x64 `.NET 8 Desktop Runtime`。项目已使用 `8.0.29` 验证；[版本、官方下载地址和 SHA-512](runtime/dotnet-desktop-runtime-8.0.29-win-x64/README.md) 单独记录在仓库中。
3. 双击 `LubanDesktopPet.exe`。程序是框架依赖单文件，EXE 不包含 .NET 运行时。
4. 退出时请在 Windows 通知区域右键小鲁班图标，选择“退出小鲁班”。

> 当前 EXE 没有商业代码签名。从网络下载后，Windows SmartScreen 可能显示未知发布者提示。请先确认 Release 来源；如发布说明提供 SHA-256，再核对哈希后运行。

## 主要功能

- 4 种点击动作：委屈、欢呼卖萌、点赞和吃饼干。每次有效点击都会随机选择，并排除上一次成功播放的动作；空闲时约每分钟从这四种动作和打呼噜待机中独立随机选择一次。许愿星、跑步、打哈欠、旧蝴蝶、“嗨～我在这里！”挥手及普通“让我认真想一想……”动作已移除；右键打开任务面板仍保留完整托腮思考姿势。
- 趴枕头打呼噜待机，鼻尖只显示一个半透明呼吸泡泡。
- 完全待机时，人物左上角显示可点击的萌太阳；进入打工后用 `420 ms` 轻压、旋转、弹入和柔光交叉动画原位变成萌月亮，退出时连续反向切回太阳。两个入口都是无文字矢量图标，不增加位图内存；96 帧循环按 8 次不等间隔安排四指落键，工作中单击不打断动作，双击才会连续切入至少 4 秒的认真打字。
- 拖到屏幕左、右、下边缘后播放探头动画；人物可拖到当前屏幕工作区最上沿，但顶部不吸附、不探头。
- 默认每次空闲约 10 分钟触发带铃铛和竹筒的大头熊猫坐骑巡游，支持双屏、负坐标副屏和不同 DPI。
- 待办事项支持新增、完成后自动移至列表底部、修改、删除、正文复制和拖拽排序。
- 定时任务精确到秒，支持单次或每 N 分钟/小时/天循环、每日免打扰、漏提醒计数和分页提醒。
- 桌宠大小可在 75%～140% 之间连续调节；绕屏开关、大小和任务数据会持久化。
- 当前用户级开机自启，不需要管理员权限。

## 操作速查

| 操作 | 结果 |
| --- | --- |
| 左键单击小鲁班 | 从 4 种可爱动作中随机播放一种，且不连续重复 |
| 完全待机时点击左上角太阳 | 坐到电脑前开始打字；左、右、下普通吸附状态不会显示该入口 |
| 打工时左键单击小鲁班 | 保持当前打字动作、速度和表情，不出现额外手掌或敲头动作 |
| 打工时左键双击小鲁班 | 立即把当前动作连续加速到 2 倍；在最近的精确中性接缝用约 133 ms 换成认真眉形和专注嘴形，再完整保持至少 4 秒认真打字；已在认真状态时会续期 |
| 打工时点击左上角月亮 | 平滑收工并回到趴枕头待机 |
| 按住左键拖动 | 移动桌宠；普通状态松手后直接判断左、右、下吸附，打工状态命中这三边时跳过普通收工和待机，直接交给同一边的真实探头状态 |
| 右键单击小鲁班 | 以专用托腮思考姿势打开或收起“待办事项 / 定时任务”面板 |
| 巡游中左键单击 | 在当前位置平滑下坐骑并回到待机，不追加点击动作 |
| 巡游中开始拖动 | 立即结束巡游的位置控制并由鼠标接管 |
| 巡游中右键单击 | 在当前位置平滑下坐骑，然后打开默认待办页 |
| 待办输入框按 `Enter` | 新增待办 |
| 选中待办正文后按 `F2` | 打开独立修改窗口 |
| 拖动待办左侧 `≡` | 调整待办顺序 |
| `Ctrl+C / Ctrl+X / Ctrl+V` | 在可编辑或可选择文字中复制、剪切、粘贴 |
| 提醒窗口点击“知道啦”、`×` 或按 `Esc` | 确认当前实际载入的提醒批次 |
| 任务面板点击“退出”，或通知区域右键小鲁班图标 | 正常保存并退出程序 |

更完整的交互、定时规则、免打扰和常见问题见 [用户指南](docs/USER_GUIDE.md)。

## 本地数据与隐私

所有业务数据默认保存在当前 Windows 用户目录：

| 位置 | 内容 |
| --- | --- |
| `%LocalAppData%\LubanDesktopPet\todos.json` | 待办文字、完成状态和顺序 |
| `%LocalAppData%\LubanDesktopPet\scheduled-tasks.json` | 定时任务、循环规则、下一次时间和免打扰设置 |
| `%LocalAppData%\LubanDesktopPet\settings.json` | 绕屏开关和桌宠大小 |
| `HKCU\Software\Microsoft\Windows\CurrentVersion\Run` 的 `LubanDesktopPet` | 当前用户开机自启命令；不写入 `settings.json` |

- 程序不上传这些数据，也不需要网络访问。
- 当前版本不创建 `log/` 文件夹或其他文件日志。
- 程序使用当前会话单实例锁；重复双击不会启动第二个进程。
- 备份前应先从托盘正常退出，再复制整个 `%LocalAppData%\LubanDesktopPet` 文件夹。恢复时也应保持程序关闭。
- 卸载时先取消“开机自启”并正常退出，再删除 EXE；本地 JSON 是否保留由用户自行决定。

## 从源码运行

日常代码修改只需要 .NET 8 SDK，不需要重新生成动画素材：

```powershell
dotnet restore .\DesktopPet.csproj
dotnet run --project .\DesktopPet.csproj
dotnet build .\DesktopPet.csproj -c Release

# 存储契约
dotnet run --project .\tests\TodoStoreChecks\TodoStoreChecks.csproj -c Release

# 完整 WPF 状态契约
dotnet run --project .\tests\UiStateChecks\UiStateChecks.csproj -c Release
```

框架依赖单文件发布：

```powershell
dotnet publish .\DesktopPet.csproj -c Release -r win-x64 `
  --self-contained false -p:PublishSingleFile=true -p:DebugType=None `
  -o .\dist
```

发布物大小必须以本次构建为准。GitHub 普通 Git 对象存在 `100 MiB`（`104,857,600` 字节）硬限制，参见 [GitHub 大文件说明](https://docs.github.com/en/repositories/working-with-files/managing-large-files/about-large-files-on-github)。`dist/**/*.exe` 和运行时安装包都已忽略；本项目始终通过 GitHub Release 附件分发 EXE，不把二进制提交到 Git 历史。

## 文档索引

- [用户指南](docs/USER_GUIDE.md)：安装、操作、待办、定时任务、备份、卸载和故障排查。
- [开发说明](docs/DEVELOPMENT.md)：源码结构、环境依赖、日常开发和仓库边界。
- [架构与状态机](docs/ARCHITECTURE.md)：窗口关系、状态优先级、渲染、缓存、多屏定位和数据流。
- [动画与图集管线](docs/ANIMATION_PIPELINE.md)：素材映射、生成、QA、Brotli 图集、时钟和内存不变量。
- [测试与发布](docs/TESTING_AND_RELEASE.md)：测试矩阵、定向入口、发布命令、哈希检查和验收清单。
- [变更记录](CHANGELOG.md)：从 `v1.0.26` 起的精选版本历史。
- [.NET Desktop Runtime 说明](runtime/dotnet-desktop-runtime-8.0.29-win-x64/README.md)。

## v1.0.65 摘要

- 删除 `star-wish`、许愿对白、常驻星星 overlay 和独立 PNG；普通动作只保留 `cry / cute / like / eat` 四种，Todo 专用 `think` 姿势保持不变。
- 左键点击由固定轮播改为独立随机选择：首次从四种动作等概率选择，此后排除上一次成功动作；启动失败不会消耗随机历史。空闲自动活动继续使用自己的“4 动作 + 1 次待机”洗牌袋，两套随机状态互不干扰。
- 左右边缘探头重做下方支撑手臂的紧凑弯曲轮廓，去掉长直紫袖和平切底边；右侧继续精确镜像左侧，底边探头保持原素材和节奏。
- 分页缓存只在真正接受新解码请求时延长空闲宽限；resident 命中、重复请求和被拒绝的预取不再反复推迟回收。人物分辨率、动作帧率和 `52 / 92 / 12 / 8 MiB` 画质预算保持不变。

## v1.0.63 摘要

- 普通蝴蝶动作和“小蝴蝶来找我了”对白已删除；旧蝴蝶资源和失败的 144 帧全人物星星方案都不进入正式资源、图集、程序集或发布包。
- 第一种普通动作改为 `star-wish`：人物直接复用现有 `cute` 的 56 张高清 smooth 帧及自然反向退场，只新增一张 `96×96` 透明星星 `Assets/luban-wish-star.png`，不复制全人物帧、不新增人物分页。
- 星星以常驻但默认透明、不可命中的 `22 DIP` overlay 显示，从画面右侧的小手边沿帽檐外缘升起，在头顶轻轻摇摆后飞走；轨迹、旋转、缩放和淡出共享现有 `CompositionTarget.Rendering + Stopwatch` 绝对时钟。
- 普通点击与空闲随机袋仍为五种动作，即 `star-wish / cry / cute / like / eat`；Todo 专用完整 `think` 入场、稳定托腮姿势和反向收起保持不变。
- 打工拖到左、右、下边缘松手后完全跳过普通 `work-exit`、枕头和 idle：目标页已驻留时从当前工作姿势原子切到 edge-rest；冷页冻结当前工作姿势直至 edge-rest resident，只有加载失败才回到待机。
- 六个正式 WPF 窗口都从 `Alt+Tab` 切换列表排除；实现不使用 `WS_EX_NOACTIVATE`，桌宠、任务和提醒仍可正常显示、输入、获得焦点、拖动和置顶。
- `star-wish` 不改变 Brotli 人物图集，清单继续为 41 页、1240 个源帧和 1240 个分页帧；分页按当前页与下一页滚动驻留，常规、巡游、稳定空闲与 LOH 阈值继续保持 `52 / 92 / 12 / 8 MiB`。

## v1.0.62 摘要

- 用轻量蝴蝶鼻尖动画替换打哈欠：蝴蝶飞近、在鼻尖附近扇翅停留后飞走，小鲁班复用现有高清思考人物帧自然歪头注视；普通点击和随机袋仍保持五种动作。
- 蝴蝶只新增一张 `96×96` 透明图，飞行、悬停和扇翅由绝对时间轴按显示器刷新率更新；不复制全人物帧，不降低人物分辨率或画质。
- 右键打开任务面板的 Todo 专用 `think` 功能、完整入场、稳定托腮姿势与反向收起全部保持不变；普通蝴蝶动作只复用像素资源，不取得 Todo 状态所有权。
- 清理 `yawn`、未播放的 `cute` loop 和不可达 `cute-smooth-057..090`，目标图集由 47 页、1454 帧收敛为 41 页、1240 帧；同时把分页预算收紧为常规 `52 MiB`、巡游 `92 MiB`、稳定空闲 `12 MiB`，正式图集仍由本次构建清单动态核验。

## v1.0.61 摘要

- 普通左键循环与空闲随机袋由 6 种收敛为 5 种，只包含打哈欠、委屈、欢呼卖萌、点赞和吃饼干；“让我认真想一想……”不再作为普通动作出现。
- 右键打开“待办事项 / 定时任务”面板时仍会完整播放并保持托腮思考姿势，Todo 专用 `think` smooth 入场不受普通动作精简影响。
- 删除不再使用的 `loop-think` 48 帧和对应分页，目标 Brotli 图集为 47 页、1454 个逻辑帧；最终数量由同次构建清单动态核验。
- 打工中拖到左、右或下边缘仍沿用 `v1.0.60` 的平滑收工与真实探头吸附交接；顶部和双屏内部接缝仍不吸附。

## v1.0.60 摘要

- 打工状态拖到当前显示器工作区的左、右、下外边缘后，先在松手位置平滑播完收工动画，再把同一边缘交给真实探头状态；不再保留“工作吸附”中间态。顶部和双屏内部接缝仍不吸附。
- 从点击循环和空闲随机袋移除“嗨～我在这里！”挥手，普通动作由 7 种收敛为 6 种；不再加载、打包或播放 `luban-wave-*` 运行时帧。
- `pic/小鲁班8.png` 继续作为作者原始素材保留，不覆盖、不删除；移除挥手后的目标图集为 48 页、1502 个逻辑帧，最终以同次构建的清单动态校验为准。
- 待办与定时任务数据格式、提醒、熊猫巡游、左/右/下探头素材和桌宠大小设置保持兼容。

## v1.0.59 摘要（历史）

- 左、右边缘贴靠恢复 `v1.0.57` 的人物素材与 `rest-first` 节奏：先保持末尾藏好姿势，再进入 10 秒探头循环；右侧继续镜像复用左侧 48 帧，下边缘不改。
- 进入左、右或下边缘吸附时会立即保存当前待办文字并关闭“待办事项 / 定时任务”任务面板，同时取消已经排队的迟到重开请求。
- 通知区域右键菜单在打开前先取得前台所有权，同一次右键产生的系统消息会合并处理；点击桌面或其他应用会正常收起，“退出小鲁班”保持整像素左对齐显示。
- 原来的“去打工 / 去睡觉”文字胶囊改为人物视觉左上角的萌太阳 / 萌月亮矢量图标；人物镜像时仍固定在屏幕左上方向，普通吸附继续隐藏太阳，工作吸附仍保留月亮用于收工。
- 打工四阶段、待办与定时任务数据格式、提醒、巡游和桌宠大小设置均保持兼容；图集仍为 53 页、1648 个逻辑帧。

## v1.0.58 摘要（历史）

- 打工状态单击改为严格的视觉无操作：不切换动作页、不改变当前相位、速度或认真期限，也不再显示额外手掌敲头；双击仍立即连续加速到 2 倍。
- 双击后的认真表情增加更明显的内收眉形和专注嘴形，双眼、人物轮廓、电脑与打字动作保持逐像素稳定；约 `133 ms` 的进入和 24 帧退出继续使用真实连续姿势。
- 打工图集彻底移除 48 帧 `work-tap` 运行时阶段，当前打工契约为 `48 / 96 / 96 / 24`，整套 Brotli 图集为 53 页、1648 个逻辑帧。
- 左右边缘贴靠后立即开始第一轮探头，不再先静止约 8.6 秒；下方手掌和前臂固定在边缘接触层，身体探出时不会把整条手臂一起裁掉。
- 待办完成自动沉底、取消完成保留位置、拖拽顺序与持久化格式保持原有行为。

## v1.0.57 摘要

- 勾选待办为完成后，只把当前项自动移动到列表最底部，其他项目保持原有相对顺序并立即持久化；取消完成保留当前位置，仍可用左侧 `≡` 自由拖拽。
- 修复左、右屏幕边缘探头帧被运行时错误平移的问题，下方支撑手臂在 48 帧动画中会始终贴住对应边缘，右侧继续镜像复用同一套素材。
- 修复通知区域右键菜单偶发显示在屏幕左上角：直接使用 Win32 光标屏幕坐标并按当前窗口 DPI 转换锚点，兼容多屏与不同缩放比例。
- 定时任务、提醒、巡游、打工动画素材和数据格式保持不变。

## v1.0.56 摘要（历史）

- 打工中的胶囊按钮由“下班啦”改为更符合角色语气的“去睡觉”，按钮内容和无障碍名称保持一致。
- 打工状态拖到当前显示器工作区的左、右、下外边缘会使用与待机相同的多屏磁吸和物理像素对齐；顶部及双屏内部接缝仍不吸附。
- 工作吸附只接管窗口位置，不切换探头帧，因此普通/认真打字动画、绝对相位、倍速和认真期限持续不变；点击“去睡觉”完成收工后，才把同一边缘交给普通探头待机。
- 未打工的左、右、下探头状态仍不显示或响应“去打工”；待办事项、定时任务、提醒、巡游和持久化数据未改动。

## v1.0.55 摘要（历史）

- 恢复用户更喜欢的 `v1.0.53` v5 打工动画，继续使用 65 张独特循环位图、9 个精确中性接缝和 `48 / 96 / 48 / 96 / 24` 帧契约。
- 打工中可以直接按住人物拖动；拖动只改变窗口位置，不退出打工、不重置当前姿势、倍速或认真状态，也不会被识别成敲头/双击。
- 打工状态拖到边缘松手不会吸附。左、右、下吸附时隐藏“去打工”，只有完全稳定的普通待机状态显示入口。
- 待办事项、定时任务、提醒、巡游和持久化数据未改动。

## v1.0.54 摘要（历史）

- v6 版本曾把普通与认真循环提高到各 81 张独特位图，并采用完整双臂关节链、8 个中性接缝和 `5.814–7.055 px` 指尖位移。
- 该版本已经发布，但用户更喜欢上一版动作，因此 `v1.0.55` 不继续使用 v6 素材；这里仅保留历史事实与 Release 对照。

## v1.0.53 摘要

- 打工序列更新为 `48 / 96 / 48 / 96 / 24` 帧；普通 96 帧循环为 `1.6 秒`，认真循环保持同一动作相位并以 2 倍速播放。
- 一圈包含 9 个像素级精确的中性接缝、65 张独特位图和 8 次不等间隔四指落键；最长中性停顿只有 5 帧，四根目标手指实测位移为 `5.528–6.049 px`。
- 手与袖口改由 v5 语义蒙版和独立关节控制点驱动，非动作区域逐像素零漂移，不再把整只手或键盘一起拉扯。
- 单击只播放 48 帧敲头并回到普通 1 倍速；双击立即以 2 倍速延续当前姿势，到最近精确接缝后播放约 `133 ms` 的认真眉过渡，再完整保持 4 秒 2 倍速认真打字。待办事项和定时任务功能未改动。

## v1.0.52 摘要

- 普通与认真打字循环均重建为 48 帧全唯一序列：普通循环以 1 倍速每 `0.8 秒`一圈，认真循环以 2 倍速每 `0.4 秒`一圈。
- 四根手指分别使用独立触键轨迹，每次只让目标手指落键；袖口、掌根、非目标手指和键盘逐帧保持静止，避免整只手掌漂移或键盘跟随变形。
- `work-enter`、`work-loop`、`work-tap`、`work-serious-loop`、`work-serious-exit` 的帧数依次为 `48 / 48 / 48 / 48 / 24`；当前 Brotli v4 图集为 53 页、1600 个逻辑帧。
- “去打工 / 下班啦”、左/右/下边缘入口、认真状态至少 4 秒及待办事项和定时任务契约保持不变。

## v1.0.51 摘要

- 普通与认真打字改为真实左右手关键姿势驱动：每个 24 帧周期依次经过中性、左手抬起落键、中性、右手抬起落键和中性，72 帧运行契约由该周期确定性重复三轮。
- 双击或单击敲头后的认真打字继续使用 2 倍速，完整手势周期为 `0.2 秒`；认真眉形使用单条连续几何过渡，不再出现双眉重影。
- 手臂只在局部预乘 Alpha 区域替换，旧手位置会完整擦除；头脸、电脑和画布逐像素锁定，普通/认真循环最低 Alpha IoU 为 `0.996528`。
- “去打工 / 下班啦”、左/右/下边缘入口、侧边支撑手臂修复及待办事项和定时任务契约保持不变。当前图集仍为 55 页、1648 个源帧和 1648 个分页帧。

## v1.0.50 摘要

- 普通打工循环改为更自然的双手交替敲键盘；人脸、电脑和帧之间的整体比例仍保持稳定。
- 单击先完整播放敲头反应，再无缝进入至少 4 秒的 2 倍速认真打字；双击直接进入认真打字，重复双击会续期。
- 左、右、下边缘吸附仍显示“去打工”；点击后先播完可见的缩回衔接，再严格从待机入口开工。顶部仍不吸附。
- 新增 72 帧 `work-serious-loop` 和 24 帧 `work-serious-exit`；当前 Brotli 分页图集为 55 页、1648 个源帧和 1648 个分页帧。待办事项和定时任务模块没有修改。

## v1.0.49 摘要

- 以用户指定的 `v1.0.48` EXE 为恢复基线，只增加独立打工状态；待办事项、定时任务、提醒和持久化模型保持不变。
- 新增头顶萌系胶囊按钮、坐在电脑前的 60 fps 打字循环、单击轻敲帽子和双击 4 秒加速；单击会等待系统双击窗口，确认不是双击后才执行。
- 打工动画继续使用同一 `CompositionTarget.Rendering + Stopwatch` 绝对时间轴，脸、双眼和电脑主体逐帧固定，不使用整图交叉淡化。
- 提醒、拖动、右键和会话切换可安全抢占打工状态；退出工作后回到稳定待机，不自动触发普通点击动作。

## v1.0.48 基线摘要

- 保持 `399×509` 渲染、全部动画帧和 `64/104 MiB` 活动峰值预算不变；稳定空闲缓存目标由 `32 MiB` 收到 `24 MiB`，实测托管空闲热集减少约 `9 MiB`。
- 静止的左、右、下边缘探头现在也能在宽限期后释放旧动作页；运动期间仍禁止回收，边缘序列页不会被误删。
- 修复相邻像素桶状态，并保持工作站 GC、硬件渲染和异步预取；只在完全空闲、淘汰债务达到门槛且受 30 秒限频保护时请求 .NET 8 反提交空闲 GC 段，不使用周期性 GC 或强制清空工作集制造表面低占用。
- 最终 EXE 实测：自动动作期间私有工作集峰值约 `421.5 MiB`，动作结束后回落到 `193–200 MiB`，接近启动空闲的 `189–191 MiB`；回收发生在空闲阶段，不减少帧数或渲染分辨率。
- 顶部拖动同时补齐动画换帧贴顶、跨 DPI 抓点和捕获失败回滚；本机真实鼠标验证仍为 `contact=0px`、`work=0px`。

最终本地发布仍只以根目录 `dist/LubanDesktopPet.exe` 为主文件。`dist/` 不进入 Git，正式对外分发仍应使用 GitHub Release 附件。

完整历史见 [CHANGELOG.md](CHANGELOG.md)。

## 许可与素材

仓库当前没有提供 `LICENSE` 或独立素材授权文件。源码、角色图片、生成素材和二进制文件的复制、修改或再分发权限不能据此自动推定；公开使用或再分发前请先向项目维护者确认授权。
