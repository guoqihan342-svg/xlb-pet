# 变更记录

本文记录自 `v1.0.26` 起的重要变化，按版本倒序排列。未列出的版本不代表不存在；GitHub Release 是否已经发布请以 [Releases 页面](https://github.com/guoqihan342-svg/xlb-pet/releases) 为准。

## v1.0.66

- 修复 `v1.0.65` 侧边探头下手后方前臂过短、看起来像缺失的问题：在保持下手抓边位置的同时，恢复一段完整、短小且连续上弯的紫色前臂，并让它自然收进脸下；不会退回 `v1.0.64` 的横向紫色长管或平切底边。
- 左侧 48 帧继续作为唯一正式源，右侧运行时精确水平镜像；头脸、帽子、耳机、双手及 Bottom 48 帧保持不变。
- 发布视觉验收必须使用真实 `190×242 DIP` 人物显示区域，在 `0.75` 与 `1.40` 用户缩放下分别检查左、右侧的浅探和深探；不能只凭 `450×550` 源图或放大联系表判断下半部分是否完整。

## v1.0.65

- 按用户要求彻底删除 `star-wish`、许愿对白、常驻星星 overlay 和独立 `Assets/luban-wish-star.png`；普通人物动作只保留 `cry / cute / like / eat`，右键任务面板专用的完整 `think` 入场和托腮姿势不变。
- 普通左键由固定轮播改为独立随机选择。首次从四种动作中选择，此后严格排除上一次成功播放的动作；动作未成功启动时不提交历史。空闲自动活动继续使用独立的“四动作 + 一次待机”洗牌袋，两套随机状态互不污染。
- 重做左右屏幕边缘探头的下方支撑手臂：保留原人物头脸、帽子、耳机和双手，只把长直紫袖和平切底边收成紧凑弯曲袖口；右侧仍水平镜像同一套 48 帧，底边 48 帧保持不变。
- 优化分页缓存预取：只有真正接受新解码请求才延长空闲裁剪宽限；resident 命中、重复需求、busy 拒绝及仅在渲染回调内发布的延迟信号不再反复操作 Timer 或推迟回收。`52 / 92 / 12 / 8 MiB` 预算、全画质帧和动作时钟保持不变。

## v1.0.64

- 待办事项页的新增输入框在空白且未聚焦时显示“写下待办事项...”占位文字；输入内容或取得输入焦点后自动隐藏，避免遮挡微软输入法组合文字。
- 占位层不可命中，不影响原有点击聚焦、`Ctrl+C`、`Ctrl+X`、回车新增及 5000 字长度限制。
- 待机太阳与打工月亮不再硬切：改为共享现有 `CompositionTarget.Rendering + Stopwatch` 的 `420 ms` 萌系交叉切换，包含轻压、下沉旋转、弹入、双色柔光和单颗星光；快速反向会从当前画面连续折返，不会闪白、跳位或补播积压帧。
- 图标切换不新增位图、`DispatcherTimer`、Storyboard 或第二个渲染订阅；59/60/120/144 Hz、250 ms UI 阻塞、打工退出当帧反馈和稳态自动退订均纳入自动回归。

## v1.0.63

- 删除普通 `butterfly` 动作、“小蝴蝶来找我了”相关对白和旧蝴蝶 WPF Resource；失败的 144 帧全人物 `star-cuddle` 方案、候选帧及生成源也不进入正式资源、图集、程序集或发布包。
- 新增轻量 `star-wish` 萌动画：人物复用现有 `cute` 的 56 张高清 smooth 帧及其自然反向退场，不复制全人物素材、不新增人物分页，也不取得 Todo `think` 的状态所有权。
- 正式新增资源只有 `Assets/luban-wish-star.png`：一张 `96×96` 透明 PNG，以常驻但默认透明、不可命中的 `22 DIP` WPF overlay 显示。一颗星从画面右侧的小手边沿帽檐外缘升起，在头顶轻轻摇摆后飞走；位置、旋转、缩放和透明度共享 `CompositionTarget.Rendering + Stopwatch` 绝对时钟，不创建第二套计时器或逐帧位图。
- 普通左键循环和空闲随机袋继续保持五种动作，顺序调整为 `star-wish / cry / cute / like / eat`；右键 Todo 仍独占原有完整 `idle → wake → think` 入场、稳定托腮姿势和反向退场。
- 修复打工拖到左、右、下边缘后的视觉交接：松手命中后完全跳过普通 `work-exit`、枕头和 idle。热页在同一渲染提交中从当前工作姿势原子切到目标 edge-rest；冷页冻结当前工作姿势直至 edge-rest resident，只有目标页失败时才安全回到待机。
- 主桌宠、任务面板、提醒、确认和两个编辑窗口统一使用原生工具窗口样式并清除应用窗口样式；按 `Alt+Tab` 不再出现桌宠透明框或辅助窗口。实现不使用 `WS_EX_NOACTIVATE`，输入法、焦点、置顶、拖动和提醒交互保持正常。
- `star-wish` 不改变人物图集：Brotli v4 清单继续为 41 页、1240 个源帧和 1240 个分页帧。
- 保持分页与内存优化不回退：常规动作、熊猫巡游和稳定空闲预算仍为 `52 / 92 / 12 MiB`，空闲 LOH 淘汰债务阈值仍为 `8 MiB`，人物显示继续使用 `399×509` 全画质帧。

## v1.0.62

- 用轻量蝴蝶鼻尖动画替换普通“刚睡醒，让我伸个懒腰～”打哈欠动作；左键循环和空闲随机袋仍为五种动作，依次为蝴蝶、委屈、欢呼卖萌、点赞和吃饼干，`yawn` 不再进入运行时、程序集或最终图集。
- 蝴蝶动作复用已经通过连续性验证的 56 张 Todo `think` 高清人物帧，只新增一张 `96×96` 透明蝴蝶；飞近鼻尖、停留扇翅和飞走均由 `CompositionTarget.Rendering + Stopwatch` 绝对时间轴驱动，不为小物件复制整套人物帧。右键 Todo 仍独占原有完整 `idle → wake → think` 入场、最终托腮姿势和严格反向退场。
- 删除 `yawn` 的 smooth/loop 资源、从不播放的 `cute` loop，以及 `cute-smooth-057..090` 不可达尾段；目标图集由 47 页、1454 帧收敛为 41 页、1240 帧，正式值仍必须以同次重建的 `Assets/luban-sprite-pages.json` 动态校验。
- 精简分页常驻策略：只永久固定待机页，普通动作、熊猫巡游和稳定空闲预算分别收紧为 `52 / 92 / 12 MiB`，空闲 LOH 淘汰债务阈值降为 `8 MiB`；人物显示仍保持 `399×509`、原有动作帧质量和按刷新率绝对定位。

## v1.0.61

- 普通左键循环和空闲随机袋只保留打哈欠、委屈、欢呼卖萌、点赞与吃饼干五种动作；移除“让我认真想一想……”这一普通动作入口，但右键打开待办面板时仍完整播放并保持专用托腮思考姿势。
- 从运行时加载、程序集和 Brotli 图集中删除仅供普通思考动作使用的 `loop-think` 48 帧；Todo 继续复用完整 `think` smooth 入场。目标图集由 48 页、1502 帧收敛为 47 页、1454 帧，正式发布仍以同次构建清单为准。
- 打工状态拖到左、右或下边缘后的行为沿用 `v1.0.60`：先平滑收工，再进入同一边缘的真实探头吸附；顶部与双屏内部接缝仍不吸附。

## v1.0.60

- 打工状态拖到当前显示器工作区的左、右或下外边缘后，不再停留在“工作吸附”；松手时锁定目标边缘，在原位平滑播完收工动画，再进入同一边缘的真实探头状态。顶部和双屏内部接缝仍不吸附。
- 收工交接保留打工绝对时钟至退场启动，不经过趴枕头待机中间帧；交接完成后由标准 `EdgePeek` 接管多屏方向、镜像、边缘动画和任务面板关闭语义。
- 从点击动作循环、空闲随机袋、运行时加载、程序集和 Brotli 图集移除“嗨～我在这里！”挥手，普通动作由七种收敛为六种。
- `pic/小鲁班8.png` 作为作者原始素材继续保留，不覆盖、不删除，但不再生成、打包或播放 `luban-wave-*` 运行时帧。移除该 5 页 / 146 帧后的目标图集为 48 页、1502 个逻辑帧，发布时仍必须从最终清单动态复核。

## v1.0.59

- 按用户选择将左、右边缘贴靠素材与入口时序完整恢复为 `v1.0.57`：三边统一从末尾休息姿势进入，左右不再立即播放首轮探头；右侧仍运行时镜像左侧序列。
- 进入左、右或下边缘吸附时自动提交待办行内修改并关闭共享的待办/定时任务面板，同时取消巡游或打工退出后已经排队的面板重开请求。
- 修复通知区域右键菜单未取得前台所有权导致的外点不关闭；系统回调改为 Dispatcher 合并打开，关闭后通过 `NIM_SETFOCUS + WM_NULL` 归还通知区焦点，并补齐整像素左对齐布局。
- 移除“去打工 / 去睡觉”可见文字胶囊，以人物左上角的萌太阳 / 萌月亮纯矢量图标原位切换；普通吸附隐藏太阳，工作吸附保留月亮，镜像人物时图标仍保持视觉左上和正向。
- 保持 `work-tap` 已删除、打工单击严格无视觉变化、双击认真模式、待办完成沉底及 53 页 / 1648 帧图集契约不变。

## v1.0.58

- 打工状态单击改为严格的视觉无操作：不显示敲头手掌，不切换动画页，也不改变打字相位、速度、普通/认真状态或认真期限。
- 双击仍立即连续加速到 2 倍，并在最近精确中性接缝切入更明显的内收眉形和专注嘴形；双眼、人物轮廓、电脑和手部动作保持稳定。
- 从运行时、程序集和 Brotli 图集中移除 48 帧 `work-tap` 阶段；当前打工帧契约为 `48 / 96 / 96 / 24`，整套图集为 53 页、1648 个逻辑帧。
- 左右边缘吸附后立即播放第一轮探头，不再先等待约 8.6 秒；下方手掌与前臂改为稳定接触层，身体探出时不再把整条手臂裁掉。
- 历史 `fix_edge_side_arm_reveal.py` 改为只读的失败关闭迁移桩，旧命令会明确退出且不再原地拉伸当前正式素材。
- 待办完成自动移至底部、取消完成保留位置、拖拽顺序及数据格式保持不变，并纳入本次完整回归。

## v1.0.57

- 勾选待办为完成后，该项自动移到列表最底部，其余项目保持原有相对顺序；连续完成多项时按完成顺序追加到底部，状态与顺序立即持久化。
- 已在末尾的完成项不会产生无意义的二次移动；取消完成只更新状态并保留当前位置，仍可手动拖拽排序。
- 修复左右屏幕边缘探头的下方支撑手臂与边缘之间出现透明缝隙；保留图集清单的 `-2px` 透明 gutter，不再由运行时错误二次平移。
- 修复通知区域右键“退出小鲁班”菜单偶发出现在屏幕左上角；菜单改用 Win32 光标屏幕坐标和 WPF DPI 转换后的明确锚点。
- 定时任务、提醒、巡游、打工动画素材和数据格式保持不变。

## v1.0.56

- 打工中的按钮文字由“下班啦”调整为“去睡觉”，显示内容与无障碍名称同步更新。
- 打工状态拖到屏幕工作区左、右、下外边缘后也会磁吸，继续保留当前工作动画、绝对相位、倍速与认真期限；顶部和双屏内部接缝仍不吸附。
- 点击“去睡觉”完成收工后，若人物仍在工作吸附位置，会把同一边缘自然交接给普通探头待机；未打工的吸附状态仍隐藏并禁用“去打工”。
- 待办事项、定时任务、提醒、巡游、动画素材和数据格式保持不变。

## v1.0.55

- 按用户反馈恢复 `v1.0.53` 的 v5 打工动画：五段序列继续使用 `48 / 96 / 48 / 96 / 24` 帧、65 张独特循环位图和 9 个精确中性接缝，不沿用 `v1.0.54` 的 v6 动作。
- 打工状态支持直接拖动。越过系统拖动阈值后只移动窗口，不退出打工、不重置普通/认真打字相位和倍速，也不会误触敲头或双击加速。
- 打工状态拖动松手后不进入边缘吸附；左、右、下吸附状态不再显示或响应“去打工”，该入口只在完全稳定的普通待机状态出现。
- 待办事项、定时任务、提醒、巡游和数据格式保持不变。

## v1.0.54

- 重做打工动画为 v6 完整双臂关节链：肩、上臂、肘、袖口、腕、掌和手指逐级联动，显著减小腕部抬升，让动作更像自然敲键盘而不是整只手砸键盘。
- 普通与认真循环各保留 96 帧并提高到 81 张独特位图；一圈使用 8 个精确中性接缝，最长连续相同画面分别为 3 帧和 2 帧。普通节奏为 6 次强触键加 2 次轻微换位，认真状态为 4 次强触键加 4 次换位并以 2 倍速播放。
- 目标指尖峰值位移为 `5.814–7.055 px`，肩、肘、腕和指尖位移严格逐级增大；专用眉形之外的脸、电脑及双臂允许区域外逐像素零漂移。
- 单击敲头、双击立即加速、约 `133 ms` 认真眉过渡、至少 4 秒认真状态及所有待办事项、定时任务和边缘交互保持不变。该动画已在 `v1.0.55` 按用户偏好回退，仅作为历史发布记录保留。

## v1.0.53

- 打工序列更新为 `work-enter 48 / work-loop 96 / work-tap 48 / work-serious-loop 96 / work-serious-exit 24`；普通循环每圈 `1.6 秒`，认真循环沿用同一相位并以 2 倍速播放。
- 96 帧循环由 65 张独特位图组成，一圈设置 9 个精确中性接缝和 8 次不等间隔的四指落键；最长中性停顿为 5 帧，四根目标手指实测峰值位移为 `5.528–6.049 px`。
- 人物手部重建为 v5 语义手/袖口蒙版：指尖、指节、掌根、手腕与袖口按人体关节层级跟随，专用眉形补丁之外的脸、头、躯干、电脑和非目标键盘区域逐像素零漂移。
- 单击只播放敲头反应并回到普通 1 倍速；双击立即把当前循环连续切到 2 倍速，到最近精确接缝后播放约 `133 ms` 的认真眉过渡，再完整保持 4 秒认真打字。待办事项、定时任务、按钮和边缘状态机未改动。

## v1.0.52

- 普通与认真打字循环均重建为 48 帧全唯一序列；普通循环以 1 倍速每 `0.8 秒`一圈，认真循环以 2 倍速每 `0.4 秒`一圈。
- 左食指、右食指、左中指和右中指分别使用独立触键轨迹；每次只移动目标手指，袖口、掌根、非目标手指和键盘逐帧保持静止。
- 打工序列帧数更新为 `work-enter 48 / work-loop 48 / work-tap 48 / work-serious-loop 48 / work-serious-exit 24`，接缝与至少 4 秒认真状态保持原契约。
- Brotli v4 分页图集更新为 53 页、1600 个逻辑帧；待办事项、定时任务、按钮和边缘状态机未改动。

## v1.0.51

- 普通与认真打字循环改为真实左右手关键姿势驱动的 24 帧完整交替周期，72 帧运行契约确定性重复三轮；左、右峰值分别使用独立手指和键位姿势，不再从同一中性手型机械拉伸。
- 单击敲头后的认真循环和双击直接加速仍保持至少 4 秒、运行时 2 倍速；每个认真手势周期为 `0.2 秒`，60Hz 显示器约呈现 12 个采样点。
- 手臂仅在局部预乘 Alpha 区域完整替换并擦除旧手，头脸和电脑锁定区逐像素零漂移；认真眉形改为每侧始终一条的连续几何过渡。
- 源素材、图集解码和接缝 QA 已同步为 24 帧周期契约；图集仍为 55 页、1648 个源帧和 1648 个分页帧。待办事项、定时任务、按钮和边缘状态机未改动。

## v1.0.50

- 普通打工动画改为自然的双手交替打字，保持人脸、电脑主体和人物比例稳定。
- 单击在系统双击判定后播放敲头反应，完成后无缝进入至少 4 秒的 2 倍速认真打字；双击直接进入认真打字，已在认真状态时重新计时至少 4 秒。
- 新增 72 帧 `work-serious-loop` 与 24 帧 `work-serious-exit`；敲头末帧、认真循环首帧、认真退出首帧和普通循环首帧使用逐像素无缝接口。
- 左、右、下边缘吸附时保留“去打工”入口；点击后先以可见连续动画退出探头，再从稳定待机进入打工。右边缘按钮文字保持正向，顶部仍不吸附。
- 修复侧边探头下方支撑手臂被边界截断的素材，并增加 `tools/fix_edge_side_arm_reveal.py` 及哈希 QA 报告，右侧继续镜像复用同一修复结果。
- Brotli v4 分页图集更新为 55 页、1648 个源帧和 1648 个分页帧；待办事项、定时任务及其数据契约未改动。

## v1.0.49

- 以用户指定的 `v1.0.48` 发布 EXE 还原出的源码和资源为唯一功能基线，未带入后续试验版本的皮肤或交互变化。
- 新增独立打工状态与头顶萌系胶囊按钮：待机可“去打工”，工作中可“下班啦”，待办事项和定时任务模块不变。
- 小鲁班坐在电脑前使用 60 fps 绝对时间轴持续敲键盘；单击经过系统双击窗口确认后播放轻敲帽子反应，双击只把原循环相位连续加速 4 秒。
- 打工期间暂停自动动作、呼噜和绕屏；拖动、右键、提醒、会话锁定和退出会取消待执行点击并按原优先级安全接管。
- 工作帧按需分页加载，脸、双眼和电脑主体保持稳定，不使用整图交叉淡化、逐帧计时器或积压帧补播。

## v1.0.48

- 不减少分辨率、帧数或动作热集：稳定空闲时把 resident 页与空闲像素桶的共同目标从 `32 MiB` 收紧到 `24 MiB`，完整保留待机页与第一张起身续页，并清掉此前额外常驻的约 `9 MiB` 空闲桶。
- 修复边缘探头长期阻塞缓存收缩：左、右、下探头只在静止保持段放行空闲裁剪与 LOH 回收，运动段仍禁止回收；当前边缘动作的完整序列页始终受保护。
- 修复相邻容量桶复用后删错空桶键的问题，并补充桶状态、24 MiB 空闲目标、静止/运动探头门禁和完整边缘页保护回归。
- 空闲淘汰债务达到 `16 MiB` 后仍只在完全静止且至少间隔 `30 秒`的后台门禁内回收，但改用 .NET 8 `Aggressive` 集合请求尽量反提交空闲 GC 段，使任务管理器中的私有工作集能随空闲热集一起回落。
- 最终 EXE 连续采样中，私有工作集由启动空闲 `189–191 MiB` 升至动作峰值 `421.5 MiB`，动作结束后回落并稳定在 `193–200 MiB`；受控剖面中的 GC 已提交内存由旧策略空闲后的 `303.61 MiB` 降到 `27.03 MiB`。
- 顶部捕获式拖动增加明确的贴顶状态：动画换帧改变接触边界时继续保持 `0px` 贴顶，向下 `1px` 立即离开；跨 DPI 后按当前物理光标和真实 HWND 重建抓取偏移，鼠标捕获失败也会完整回滚。

## v1.0.47

- 彻底移除人物拖动对系统 `DragMove` 的依赖。鼠标捕获期间按光标、真实 HWND 和目标显示器工作区的物理像素坐标实时移动，固定 `454×454` 透明包络可以越过工作区顶边，人物当前帧的真实可见顶边仍精确停在顶沿。
- 顶部继续是普通位置，不新增 `EdgeDock.Top` 或顶部探头；左、右、下释放吸附、跨屏接缝判断、待办窗口和气泡跟随保持原有行为。
- 顶部夹紧后会重新锚定抓取点，向下拖动一个像素即可立即离开；每次输入重新采样当前帧可见边界和 Per-Monitor DPI，避免混合 DPI 或动画换帧造成间隙、粘滞和坐标跳动。
- 顶部回归改为物理像素矩阵加真实 WPF 鼠标拖动：覆盖 75%/100%/125%/140% 桌宠大小、100%/125%/150% 物理计算和负坐标工作区；本机真实拖动验证为 `contact=0px`、`work=0px`、误差 `0px`。

## v1.0.46

- 保持 `399×509` 渲染分辨率、全部动画帧和普通动作 `64 MiB` 热集不变；后台解码前预先归还稍后必然淘汰的非保护 LRU 页，并允许相邻 `1 MiB` 容量桶受限复用，减少跨动作的大对象堆重复分配。
- 修复自然 Gen2 错误清空分页淘汰债务的问题；只有显式的空闲 LOH 压缩完成后才扣除债务，避免长时间运行后已淘汰像素页留下高水位。
- 熊猫巡游像素池上限从 `128 MiB` 收紧到 `104 MiB`。当前完整 boarding、flight、固定待机页及相邻桶最坏余量需要 `101 MiB`，构建测试会按清单动态校验，不减少任何巡游帧。
- 内存剖面增加普通动作全集、完整巡游、resident 页数、池中 rented/free、分配与复用次数；七种动作连续轮播必须达到实际复用门槛。

## v1.0.45

- 待办行改为与定时任务相同的状态结构：静止态保留透明一像素边框占位，悬停态使用 `#DCEBFF` 浅蓝底和 `#5B8DEF` 完整圆角描边；新增静止、悬停、移出、普通/完成项和相邻行回归，系统容器仍不得绘制额外上下横线。
- 修复固定 `454×454` 透明包络导致人物拖不到顶部的问题。系统拖动返回后按当前帧真实可见接触边界对齐当前显示器工作区顶沿；顶部继续保持普通状态，不新增 `EdgeDock.Top`，左上和右上仍可按侧边吸附。
- 修复熊猫巡游中点击的硬切和回跳：左键在最后呈现位置反向播放下坐骑，转为拖动时先释放窗口位置，右键在退场完成后再通过 Dispatcher 打开默认待办页。
- 增加[架构与状态机](docs/ARCHITECTURE.md)，并完善用户指南、开发约束、测试发布和 .NET Desktop Runtime 交付说明。

## v1.0.44

- 待办事项行悬停改为与定时任务相同的“浅色填充＋完整圆角描边”结构，配色仍为蓝色；待办 `ListBoxItem` 继续保持零边框、无系统悬停触发器，不会重新出现额外的上下横线。
- WPF 像素回归改为分别验证浅蓝内部填充与较深蓝色四边描边，并继续单独验证拖拽排序插入线。
- 重构项目文档：首页保留快速开始和当前能力，详细内容分别进入用户指南、开发说明、动画与图集管线、测试与发布；同时记录源码与 GitHub Release 可能不同步、框架依赖运行时、未签名 EXE 和本地验证边界。

## v1.0.43

- 待办行悬停时，圆角浅蓝背景与 1 像素边框引用同一个画刷，彻底消除顶部、底部残留的浅蓝横线，同时避免文字、勾选框和图标产生 1 像素位移。
- 新增真实 WPF 渲染像素回归，覆盖已完成待办的悬停边缘；拖拽排序的单条插入位置提示线继续独立验证。

## v1.0.42

- 不再在待办行悬停时把整行描边强化成高对比蓝色。该版本仍会继承基础浅蓝边框，后续由 `v1.0.43` 完成融合修复。

## v1.0.41

- 待办列表外层使用零边框、无悬停触发器的专用容器模板，避免系统容器样式叠加选中或焦点描边。

## v1.0.40

- 呼噜泡泡改用 WPF 合成动画时钟；完全静止时不再维持托管逐刷新率回调。
- 左、右、下边缘探头的长静止段使用低频唤醒；熊猫巡游预加载只保留 `250 ms` 异常看门狗。
- 图集页首次通过压缩 SHA-256 后缓存只读验证结果，重载时仍严格校验解码像素。
- 待办箭头、窗口移动、朝向和桌宠大小跳过无意义的重复属性写入。

## v1.0.39

- 新增与修改循环任务的免打扰时间统一改用时、分、秒选择器。
- 普通动作使用 `64 MiB` 滚动页预算，持续空闲收敛到 `32 MiB`；像素数组按容量桶复用。
- 提醒窗口关闭防重入标志改在输入优先级释放，避免下一次有意关闭被渲染延迟吞掉。

## v1.0.38

- 修改定时任务时，免打扰开始和结束改用与提醒时间一致的萌橘色选择器，支持秒级和跨夜时段。
- `Esc` 优先收起内部选择器，不会误取消整个修改窗口。

## v1.0.37

- 待办修改窗口增加“仅点击‘确定修改’保存 · Esc 取消”提示，并保留 `0/5000` 字数统计。

## v1.0.36

- 取消额外自动彩蛋及其后台计时、预加载和多屏定位逻辑。
- 保留七种点击动作、熊猫坐骑巡游、左/右/下边缘吸附、待办和定时提醒。

## v1.0.33

- 镜像、旋转和待办切换使用的整帧变换缓冲改为与图集解码共享按需缓冲池，不改变图片分辨率、帧数、速度和刷新率。
- 动画片段复用帧名，减少重复字符串；提醒期间关闭修改窗口会立即释放旧视觉树引用。

## v1.0.32

- 活动期间使用 `128 MiB` 解码页软预算，动作结束保留 `20 秒` 热缓存，稳定空闲收缩到 `80 MiB`。
- 只有完全空闲且满足阈值时才低频请求 Gen2/大对象堆整理，避免渲染期卡顿。

## v1.0.31

- 提醒窗口改为每页最多 5 条，当前批最多加载 100 条，避免窗口无限向上堆叠。
- “本批都知道啦”、右上角 `×`、`Esc` 和系统关闭统一确认当前批次；超过 100 条的余量继续显示。

## v1.0.30

- 关闭提醒窗口会清空已经显示的旧提醒并按实际次数推进循环，未显示余量不会丢失。
- 待办和定时任务输入框为微软雅黑文字增加下沿空间并进行物理像素对齐。

## v1.0.29

- 循环任务可以单独开启每日免打扰，默认 `22:00:00–07:00:00`，支持精确到秒和跨午夜。
- 静默期间不弹窗、不播放喇叭动画、不推进任务；结束后按原节奏汇总提醒。
- 免打扰配置持久化到 `scheduled-tasks.json`。

## v1.0.28

- 点击气泡箭头按真实物理屏幕坐标重新翻转，支持负坐标副屏和系统自动换边。
- 修复正文列与箭头列未同步交换的问题。

## v1.0.27

- 待办删除使用蓝色风格，定时任务删除使用橘红色风格。
- 删除确认窗改为透明无系统边框窗口，并清理编辑窗圆角处的暗边和露白。

## v1.0.26

- 主桌宠窗口固定使用最大透明包络，右键、收起和状态切换不再反复改变原生窗口尺寸。
- 待办使用蓝色勾选框和圆角输入框；底部设置随当前选项卡切换蓝色或橘色主题。
- 待办编辑窗与定时任务编辑窗使用独立默认尺寸，均支持拖拽调整大小。
